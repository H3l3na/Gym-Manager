<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:vm="clr-namespace:GymManager3.MobileApp.ViewModels"
             mc:Ignorable="d"
             x:Class="GymManager3.MobileApp.Views.NewLoginPage">
    <ContentPage.BindingContext>
        <vm:NewLoginViewModel />
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Button Command="{Binding RegisterCommand}" Text="Register" BackgroundColor="LightSkyBlue" BorderColor="LightGray" TextColor="White"></Button>
            <Label Text="Login as Trainer:" />
            <Button Command="{Binding LoginTrenerCommand}" Text="Login" ></Button>
            <Label Text="Login as Member:" />
            <Button Command="{Binding LoginPolaznikCommand}" Text="Login" ></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>